import { iconsMap } from './helpers'

export const options = {
  sizes: ['sm', 'md', 'lg', 'xl'],
  names: Object.keys(iconsMap),
  colors: ['base', 'highlight', 'muted', 'primary', 'inverted'],
  backgrounds: ['highlight', 'inverted'],
}
